<%= turbo_stream.replace "store_reviews",
                     partial: "stores/store_reviews",
                     locals: {
                       reviews: @reviews,
                       pagy_reviews: @pagy_reviews,
                     } %>
<%= turbo_stream.replace @store.reviews.new,
                     partial: "store_reviews/add_review_button",
                     locals: {
                       store: @store,
                     } %>
