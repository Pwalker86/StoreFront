<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "MarketHive" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap"
      rel="stylesheet"
    >
    <script src="https://kit.fontawesome.com/b333939790.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%#= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <!-- move optimistic-updates to wrap each cart item qty incrementor, then add
  outlet to cart_count -->
  <body data-controller="optimistic-updates">
    <% if current_store_admin %>
      <%= render "layouts/admin_navbar" %>
    <% else %>
      <%= render "layouts/navbar" %>
    <% end %>
    <%= render "layouts/toast" %>
    <p style="color: red" class="alert"><%= alert %></p>
    <%= yield %>
    <%= render "layouts/footer" %>
  </body>
</html>
