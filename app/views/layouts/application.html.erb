<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <title><%= content_for(:title) || "MarketHive" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap"
      rel="stylesheet"
    >
    <script src="https://kit.fontawesome.com/b333939790.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="flex flex-col min-h-screen bg-gray-50">
    <% if current_store_admin %>
      <%= render "layouts/admin_navbar" %>
    <% else %>
      <%= render "layouts/navbar" %>
    <% end %>
    <%= render "layouts/toast" %>
    <p style="color: red" class="alert"><%= alert %></p>
    <main class="flex-grow w-full mx-auto py-8 px-4 sm:px-6 lg:px-8">
      <%= yield %>
    </main>
    <%= render "layouts/footer" %>
  </body>
</html>
