<% if current_user.is_a? User %>
  <%= turbo_frame_tag product.reviews.new do %>
    <%= link_to "Add Your Review",
    new_store_product_product_review_path(product.store, product),
    class:
      "inline-flex items-center px-4 py-2 border border-transparent rounded-md font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 text-sm transition-colors duration-150" %>
  <% end %>
<% end %>
