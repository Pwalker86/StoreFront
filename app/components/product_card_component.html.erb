<div class=" ProductCard rounded-lg p-1.5 shadow-lg bg-slate-200">
  <%= link_to [@product.store, @product],  class: "ProductCard__link", data: {turbo:  false} do %>
    <div class="ProductCard__content">
      <% if @product.images.any? %>
        <%= image_tag @product.images.first.variant(:thumb),
        class: "ProductCard__image rounded-lg" %>
      <% end %>
    </div>
  <% end %>
  <h3 class="ProductCard__name"><%= @product.name %></h3>
  <p class="ProductCard__price"><%= number_to_currency(@product.price) %></p>

  <div class="flex justify-between">
    <%= button_to "Add To Cart",
    user_cart_path(helpers.active_user),
    params: {
      user_entity: helpers.active_user.class.name,
      product_id: @product.id,
      quantity: 1,
    },
    data: {
      turbo: false,
    },
    method: :patch,
    class: "button" %>
    <%= link_to "View More",
    [@product.store, @product],
    data: {
      turbo: false,
    },
    class: "button" %>
  </div>
</div>
